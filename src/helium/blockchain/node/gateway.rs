pub struct Gateway {
    pub address: String,
    pub h3_index: String,
    pub lat: String,
    pub lng: String,
    pub blocks_mined: u64,
    pub score: i32,
    pub last_proc_challenge: String,
    pub status: String,
}